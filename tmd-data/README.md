# tmd-data

`tmd-data` contains the transport-neutral scalar and ordered-table types shared
by Tanu Markdown data-source adapters and computation engines.

The crate deliberately has no knowledge of `.tmd` containers, manifests,
SQLite, rendering, or editor protocols. It is an internal, pre-1.0 workspace
crate and is not currently publishable.
