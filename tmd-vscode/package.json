{
  "name": "tanu-markdown-editor",
  "displayName": "Tanu Markdown Editor",
  "description": "Secure custom editor for Tanu Markdown .tmd and .tmdp documents.",
  "version": "0.0.1",
  "private": true,
  "publisher": "tanu-markdown",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Nagitch/tanu-markdown.git",
    "directory": "tmd-vscode"
  },
  "bugs": {
    "url": "https://github.com/Nagitch/tanu-markdown/issues"
  },
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=24"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCustomEditor:tanuMarkdown.editor",
    "onCommand:tmd.newDocument",
    "onCommand:tmd.validate",
    "onCommand:tmd.addAttachment",
    "onCommand:tmd.removeAttachment",
    "onCommand:tmd.exportHtml",
    "onCommand:tmd.convert"
  ],
  "contributes": {
    "customEditors": [
      {
        "viewType": "tanuMarkdown.editor",
        "displayName": "Tanu Markdown Editor",
        "selector": [
          {
            "filenamePattern": "*.tmd"
          },
          {
            "filenamePattern": "*.tmdp"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "tmd.newDocument",
        "title": "TMD: New Document"
      },
      {
        "command": "tmd.addAttachment",
        "title": "TMD: Add Attachment"
      },
      {
        "command": "tmd.removeAttachment",
        "title": "TMD: Remove Attachment"
      },
      {
        "command": "tmd.validate",
        "title": "TMD: Validate Document"
      },
      {
        "command": "tmd.exportHtml",
        "title": "TMD: Export HTML (self-contained)"
      },
      {
        "command": "tmd.convert",
        "title": "TMD: Convert Container Format"
      }
    ],
    "configuration": {
      "title": "Tanu Markdown",
      "properties": {
        "tanuMarkdown.cliPath": {
          "type": "string",
          "default": "tmd",
          "description": "Path to the installed Tanu Markdown CLI executable."
        },
        "tanuMarkdown.timeoutMs": {
          "type": "number",
          "default": 15000,
          "minimum": 1000,
          "maximum": 300000,
          "description": "Maximum time in milliseconds for each CLI operation."
        }
      }
    }
  },
  "main": "./dist/extension.js",
  "files": [
    "dist/*.js",
    "README.md",
    "LICENCE"
  ],
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p .",
    "check": "tsc -p . --noEmit",
    "test": "npm run compile && node --test dist/test/*.test.js",
    "pack": "npm run compile && vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/vscode": "1.90.0",
    "@types/node": "^26.1.2",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^7.0.2"
  }
}
